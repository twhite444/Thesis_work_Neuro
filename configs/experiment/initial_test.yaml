# @package _global_

defaults:
  - override /model: small_net
  - override /preprocessing: none
  - override /training: quick_test
  - override /data: test_data

experiment_name: "initial_test"

# Override for very quick testing
training:
  max_epochs: 5
  early_stopping:
    patience: 3
  
data:
  batch_size: 16
