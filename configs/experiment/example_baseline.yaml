# ==================================================================
# Example Baseline Experiment
# ==================================================================
# A standard baseline experiment to compare against
# ==================================================================

experiment_name: baseline

defaults:
  - /model: medium_net
  - /data: olfactory_default
  - /preprocessing: pca_default
  - /training: default
  - _self_

seed: 42

# This is a baseline - use defaults for everything
# No customizations needed

# Expected results:
# - Training time: ~5 minutes
# - Val loss: ~0.04-0.05 (depends on data)
# - Use this as reference for other experiments
