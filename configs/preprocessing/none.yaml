# ==================================================================
# No Preprocessing Configuration
# ==================================================================
# Use raw features without any transformation
# Best for: Debugging, checking if preprocessing helps
# ==================================================================

enabled: false  # Completely disable preprocessing

# When enabled=false, all methods below are ignored
methods: []

# Use case:
# - Testing if PCA is helpful
# - Debugging feature extraction
# - When features are already well-prepared
# - Interpretability (want original features)

# Speed: Instant (no computation)
# Memory: Higher (more features)
# Interpretability: High (original features)
